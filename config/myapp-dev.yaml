# ==============================================================================
# 扁平命名：<project>-<env>.yaml 示例
# ==============================================================================
# 复制为 lighthouse-dev.yaml 或你的 <project>-<env>.yaml
# ==============================================================================

global:
  project_name: "deploy-engine-demo"
  k3s:
    apiServer:
      domain: "k3s-demo.example.com"
