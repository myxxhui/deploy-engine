# ==============================================================================
# 扁平命名：default-<env>.yaml（无 project 时使用）
# ==============================================================================
# 复制为 default-dev.yaml 或 <project>-<env>.yaml（如 lighthouse-dev.yaml）
# ==============================================================================

global:
  project_name: "deploy-engine-demo"
  k3s:
    apiServer:
      domain: "k3s-demo.example.com"
