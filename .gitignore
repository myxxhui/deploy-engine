# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
# *.tfstate.*
*.tfstate.backup
# Terraform 自动生成的带时间戳的 state 备份（如 terraform.tfstate.1234567890.backup）
*.tfstate.*.backup
*.tfstate.bak.*

# Local config with secrets
# config/examples/ 为示例目录，可安全提交 GitHub（使用占位符 CHANGE_ME）
config/terraform-*.tfvars

# Build
bin/
*.exe

# State
.deploy/

# IDE / OS
.idea/
.vscode/
.DS_Store

# .cursorrules: 允许提交，供本仓 AI 规则共享（见 diting-doc 00_系统规则 §十一）
